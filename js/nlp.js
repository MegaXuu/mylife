/* ==========================================================================
   nlp.js — parseur français de saisie rapide (« arroser le ficus tous les
   3 jours », « impôts le 15 mai »…). Placeholder : logique réelle au Lot 6.
   Pas d'écran propre : renderNlp() renvoie juste un fragment.
   ========================================================================== */
function renderNlp(){
  return emptyState('bientôt : saisie en langage naturel.', 'nlp');
}
