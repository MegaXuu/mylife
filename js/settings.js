/* ==========================================================================
   settings.js — réglages, profil, export/import JSON, à propos,
   réinitialisation. Placeholder : logique réelle au Lot 11.
   ========================================================================== */
function renderSettings(){
  document.getElementById('s-settings').innerHTML =
    '<h1>Réglages</h1>'+
    emptyState('bientôt : profil, préférences, export et import.', 'settings');
}
